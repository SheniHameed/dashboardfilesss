import React from "react"




const Tabs = (props: any) => {
	const {userName} = props.props
	return (
		<div className="tabs">
			<h1>REPAIRING FROM OF CLIENT{userName}</h1>

			
		</div>
	)
}

export default Tabs
